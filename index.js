import fiberwalker from "./fiberwalker";
import dependenciesGraph from "./parser";

export default function fetchTree(entryFile) {
  //dependenciesFile run with entryFile
  dependenciesGraph(entryfile);
  //

  //
}
